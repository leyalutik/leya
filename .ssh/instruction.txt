# Open git-bash

cd ~/leya/.ssh
eval "$(ssh-agent -s)"
ssh-add id_ed25519

# IF nesceseraly Add a (???)new SSH key to your GitHub account

#in cmd.exe
#Copy the SSH public key to your clipboard.
clip < id_ed25519.pub

# Add SSH key to Setting of github account

git remote set-url origin git@github.com:leyalutik/leya.git