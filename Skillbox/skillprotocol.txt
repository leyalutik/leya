11.Функции 
Пройдено

Ответ куратору.

Как сейчас по новому стандарту С++ обявлть массив


Видео урок
-----------------------------------------------------------------------------------
12.Массивы
1 Урок. Инициализация
int month[12];

//std::array<int> m(12); Вопрос куратору как лучше


2.Урок.
int months[] = {31,28,31,30};
int months[12];
int months[12] = {1,2,3, ...};
cout << months; //адрес первой элемента массива

3.Урок. Алгоритмы на массивах
	Задача1. 10 спортсменов по очереди бегут определённую 	дистанцию за определенную время .time[i];
Вопрос, кто занял 1, 2.3 места.
	Приём нахождения индекстов.

	Задача2. Известный спортсмен. Принимать витамины в течении дня в строго отведённые часы. 6 видов витаминов. Каждый  вид раз в 4 часа. 00:00 04:00 08:00 12:00 16:00 20:00.
В программе хранится порядок приёма таблеток.
Но в других странах ему постоянно приходится переводить часы.
Считаем что приём таблеток спортсмен начинает в родной стране,
а потом перелетает.
	Перебор элементов

Смещение влево
int first = m[0];
for(int i=0; i< n-1; i++)
{
	m[i] = m[i+1];
}
m[n-1] = first;

Смещение вправо
int last = m[n-1];
for(int i=n-1; i>0; --i)
{
    a[i] = a[i-1];
}
a[0] = last;

4 Урок Assert

assert(истина) программа идёт дальше
assert(ложь) аварийное завершение
	и выведено название исходника и номер строки где что-то не так.

1.При сборке релизной версии все assert будут пустыми строками, почему
2.Используется только при отладке
3.Более быстро пишется чем if else

Помогает находить логические ошибки при разработке


------------------------------------------------------------------------------------
Задача 1.
Дано 10 фамилий жильцов.
Дано 10 номеров квартив с 1 по 10

Пользователь вводит 3 номера квартиры.
Нужно вывести в консоль фамилию жильца этой квартиры.
Если введёт некорректный номер квартиры > сообщение об ошибке.

Структура

main.cpp

task1.cpp
task1.h

tests.cpp
tests.h

test_framework.h


CMakeists.txt

По логике куда добавляются исходники и заголовки.
